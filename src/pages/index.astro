---
import Sidebar from '../components/Sidebar.astro';
import Layout from '../layouts/Layout.astro';
import ErfaringComponent from '../components/Erfaring.tsx';
import { jobber } from '../data/erfaring';
import Utdanning from '../components/Utdanning.astro';
import Teknologier from '../components/Teknologier.astro';
import Prosjekter from '../components/Prosjekter.astro';

// Konfigurer hvor mange jobber som skal vises initialt
const ANTALL_SYNLIGE_JOBBER = 2;
---

<Layout>
	<div class="container">
		<aside class="sidebar">
			<Sidebar />
		</aside>
		<main class="content">
			<ErfaringComponent 
				client:load 
				jobber={jobber} 
				antallSynlige={ANTALL_SYNLIGE_JOBBER} 
			/>
			<Prosjekter />
			<Utdanning />
			<Teknologier />
		</main>
	</div>
</Layout>

<style>
	.container {
		display: grid;
		grid-template-columns: 350px 1fr;
		grid-template-areas: 'sidebar content';
		max-width: 1200px;
		margin: 0 auto;
		min-height: 100vh;
		gap: 3rem;
		padding-top: 3rem;
	}

	.sidebar {
		grid-area: sidebar;
		box-sizing: border-box;
		height: 100vh;
		position: fixed;
		top: 0;
		width: 350px;
		padding: 3rem 2rem;
		overflow-y: auto;
	}

	.content {
		grid-area: content;
		box-sizing: border-box;
		padding: 2rem;
		display: flex;
		flex-direction: column;
		gap: 5rem;
	}

	@media (max-width: 900px) {
		.container {
			grid-template-columns: 1fr;
			grid-template-areas:
				'sidebar'
				'content';
			padding-top: 1rem;
			gap: 1rem;
		}

		.sidebar {
			position: static;
			height: auto;
			width: 100%;
			padding: 1rem 3rem;
		}

		.content {
			padding: 1rem 3rem;
		}
	}

	@media (min-width: 769px) {
		.sidebar {
			left: max(2rem, calc(50% - 600px));
		}
	}

	@media (min-width: 769px) and (max-width: 1240px) {
		.sidebar {
			left: 2rem;
		}
	}
</style>
